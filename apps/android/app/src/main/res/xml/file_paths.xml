<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures written to filesDir/captures -->
    <files-path name="captures" path="captures/" />
    <!-- Staged upload files -->
    <files-path name="upload_staging" path="upload_staging/" />
    <cache-path name="cache" path="." />
</paths>
