# Yarn Classic (v1) config.
# Match pnpm's prior leniency: don't hard-fail on transitive `engines` ranges
# (e.g. deps requiring Node ^22.13 while local dev runs 22.12). Docker's node:22
# base image already satisfies these, so this only affects local installs.
--ignore-engines true
